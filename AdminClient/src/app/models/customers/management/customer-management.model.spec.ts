import { CustomerManagement.Model.Ts } from './customer-management.model.ts';

describe('CustomerManagement.Model.Ts', () => {
  it('should create an instance', () => {
    expect(new CustomerManagement.Model.Ts()).toBeTruthy();
  });
});
