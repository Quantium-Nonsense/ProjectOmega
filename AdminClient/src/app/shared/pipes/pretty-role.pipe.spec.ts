import { PrettyRolePipe } from './pretty-role.pipe';

describe('PrettyRolePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyRolePipe();
    expect(pipe).toBeTruthy();
  });
});
